{
  "name": "flitterbot",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.13.1",
  "scripts": {
    "prepare": "lefthook install",
    "control-surface": "node --experimental-strip-types src/server.ts",
    "test": "node --experimental-strip-types --test tests/*.test.ts && pnpm --dir web test",
    "web:dev": "pnpm --dir web run dev",
    "web:build": "pnpm --dir web run build",
    "audit": "pnpm run audit:ts && pnpm run audit:shell",
    "audit:ts": "biome check src/ web/src/ && tsc --noEmit && tsc --noEmit -p web/tsconfig.json && knip --include files,dependencies,unlisted --exclude binaries --no-config-hints && knip --directory web --include files,dependencies,unlisted --exclude binaries --no-config-hints",
    "audit:ts:fix": "biome check --fix src/ web/src/ && pnpm run audit:ts",
    "audit:shell": "find installer -type f \\( -name '*.sh' -o -path 'installer/bin/flitterbot-up' -o -path 'installer/bin/flitterbot-wa' \\) -print0 | xargs -0 -n1 bash -n && node --check installer/install.mjs && node --check installer/uninstall.mjs",
    "up:latest": "pnpm update $(jq -r '(.dependencies // {}) + (.devDependencies // {}) | to_entries[] | select(.value == \"latest\") | .key' package.json)"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.5",
    "@types/node": "^25.9.5",
    "knip": "^6.29.0",
    "lefthook": "^2.1.10",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "@earendil-works/pi-ai": "latest",
    "@earendil-works/pi-coding-agent": "latest",
    "@ff-labs/fff-node": "^0.7.2",
    "@whiskeysockets/baileys": "7.0.0-rc13",
    "dotenv": "^17.4.2",
    "openai": "latest",
    "pino": "^10.3.1",
    "qrcode-terminal": "^0.12.0"
  },
  "knip": {
    "entry": [
      "src/whatsapp/cli.ts",
      "src/whatsapp/daemon.ts",
      "src/server.ts"
    ],
    "project": [
      "src/**/*.ts"
    ],
    "ignore": [
      "src/**/index.ts",
      "web/tests/**"
    ]
  }
}
