# Flitterbot Agent Instructions

<rule id="must-fetch-user-preferences">
When you spot one of the situations below, if not already aware of the specific memory associated, pls run a recall with `python3 ~/.flitterbot/skills/learnings/scripts/recall.py show {2-digit-code}` — it pulls in lazy-loaded important instructions that guide how you handle it.

Example:
- `python3 ~/.flitterbot/skills/learnings/scripts/recall.py show CC` — trigger/scenario description
</rule>
