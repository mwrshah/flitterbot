# Flitterbot Agent Instructions

<rule id="must-fetch-user-preferences">
For any of the situations below, your first move should be `python3 ~/.flitterbot/skills/learnings/scripts/recall.py show {2-digit-code}` — it pulls in lazy-loaded important instructions that guide how you handle it.

Example:
- `python3 ~/.flitterbot/skills/learnings/scripts/recall.py show CC` — trigger/scenario description
</rule>
