# Flitterbot environment variables
# Copy this file to .env and fill in your keys.
# The control surface loads .env automatically on startup via dotenv.

# Optional — add any Pi-supported provider's standard API-key environment variable.
# OAuth can instead be configured from the Surface settings cog under Accounts & Model providers.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
