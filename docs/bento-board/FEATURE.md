# Bento Board

A Bento-style variable-size grid layout for the stream agent web UI, replacing the flat list of streams and sessions that becomes unnavigable at scale. Cards occupy different amounts of screen real estate based on state, with gap-minimized responsive layout and smooth animated transitions.
