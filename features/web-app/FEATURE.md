# Web App

A thin browser client for Autonoma providing a single pane of glass to chat with the stream agent, browse sessions, inspect transcripts, direct-message sessions, and monitor the runtime. It replaces the limitations of terminal-only access where transcripts are raw JSONL and no overview exists.
