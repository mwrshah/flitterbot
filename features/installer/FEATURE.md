# Installer

A permission-gated, manifest-tracked installer/uninstaller for deploying the `~/.autonoma/` runtime tree and modifying external configs (like `~/.claude/settings.json` and scheduler units). Every change is tracked so the entire installation can be cleanly reversed in one command.
