# @-Mention Directory Autocomplete

An `@`-mention triggered filesystem path autocomplete popover in the web chat input. It shows the top 15 matching entries from the machine's filesystem, supports keyboard navigation, and provides progressive directory traversal so users can reference files without typing full paths from memory.
