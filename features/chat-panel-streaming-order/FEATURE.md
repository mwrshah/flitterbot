# Chat Panel Streaming Order

Fix the chat panel so that messages during a stream agent turn appear in true chronological order — text, tool calls, tool results, and more text interleaved as they actually occur. Previously, completed messages bunched at the top and streaming content pinned at the bottom, breaking the visual chronology.
