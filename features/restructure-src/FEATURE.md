# Restructure Src

Restructure the `src/` directory to reduce excessive nesting (3+ levels) and mixed organizational strategies. The goal is a maximum of 2 levels of nesting organized by domain (bounded context) rather than by layer, making navigation faster and imports less verbose.
